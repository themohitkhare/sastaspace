{
  "name": "threadly",
  "private": true,
  "version": "0.1.0",
  "description": "Threadly — AI wardrobe stylist. Marketing site + waitlist backend.",
  "packageManager": "pnpm@9.12.0",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev:web": "npx serve web",
    "dev:waitlist": "pnpm --filter @threadly/waitlist dev",
    "deploy:waitlist": "pnpm --filter @threadly/waitlist deploy"
  }
}
