FROM golang:1.23 AS builder
WORKDIR /src
COPY . .
RUN go mod download && CGO_ENABLED=0 GOOS=linux go build -o /out/api .

FROM gcr.io/distroless/static
COPY --from=builder /out/api /api
EXPOSE 8080
ENTRYPOINT ["/api"]
