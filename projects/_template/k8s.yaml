apiVersion: apps/v1
kind: Deployment
metadata:
  name: __NAME__-web
  namespace: projects
spec:
  replicas: 1
  selector:
    matchLabels:
      app: __NAME__-web
  template:
    metadata:
      labels:
        app: __NAME__-web
    spec:
      containers:
        - name: web
          image: localhost:32000/sastaspace-__NAME__-web:latest
          ports:
            - containerPort: 3000
---
apiVersion: v1
kind: Service
metadata:
  name: __NAME__-web
  namespace: projects
spec:
  selector:
    app: __NAME__-web
  ports:
    - port: 80
      targetPort: 3000
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: __NAME__
  namespace: projects
spec:
  ingressClassName: nginx
  rules:
    - host: __NAME__.sastaspace.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: __NAME__-web
                port:
                  number: 80
