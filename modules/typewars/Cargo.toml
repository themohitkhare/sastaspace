[package]
name = "typewars-module"
version = "0.1.0"
edition = "2021"
publish = false

[lib]
crate-type = ["cdylib"]

[dependencies]
spacetimedb = "2.1"
log = "0.4"

# [profile.release] intentionally omitted — see modules/sastaspace/Cargo.toml.
