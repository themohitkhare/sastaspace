// Stub — fleshed out by Phase 1 W4 (moderator-agent).
import type { StdbConn } from "../shared/stdb.js";

export async function start(_db: StdbConn): Promise<() => Promise<void>> {
  throw new Error("moderator-agent not implemented — Phase 1 W4 deliverable");
}
