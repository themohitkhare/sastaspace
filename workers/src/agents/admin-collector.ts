// Stub — fleshed out by Phase 1 W2 (admin-collector).
import type { StdbConn } from "../shared/stdb.js";

export async function start(_db: StdbConn): Promise<() => Promise<void>> {
  throw new Error("admin-collector agent not implemented — Phase 1 W2 deliverable");
}
