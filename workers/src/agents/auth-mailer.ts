// Stub — fleshed out by Phase 1 W1 (auth-mailer).
import type { StdbConn } from "../shared/stdb.js";

export async function start(_db: StdbConn): Promise<() => Promise<void>> {
  throw new Error("auth-mailer agent not implemented — Phase 1 W1 deliverable");
}
