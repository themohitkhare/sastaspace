---
title: Hello, workshop.
date: 2026-04-25
summary: First note. The lab is open. Comments are on, the moderator is watching.
---

This is the first note. Mostly a placeholder so the route works — a real one is coming.

A few things that already work as you read this:

- This page is a `.mdx` file in `apps/notes/content/posts/`. Push a new file, CI deploys it.
- The comment box below writes to a SpacetimeDB table, gets classified by an Ollama-served `llama-guard3:1b` running on the lab's GPU, then appears live for everyone.
- No login. No tracking. The only thing that's recorded is the comment itself and a per-session identity for rate-limiting.

That's the whole pipeline: git, a tiny database, a tiny model, a tiny script. About as sasta as it gets.

If you spot something broken, comment below and I'll see it within seconds.
