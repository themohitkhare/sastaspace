services:
  backend:
    build:
      context: .
      dockerfile: backend/Dockerfile
    ports:
      - "${SERVER_PORT:-8080}:8080"
    env_file:
      - path: .env
        required: false
    environment:
      - CLAUDE_CODE_API_URL=http://claude-code-api:8000/v1
      - CORS_ORIGINS=http://localhost:3000
      - SASTASPACE_SITES_DIR=/data/sites
    volumes:
      - sites_data:/data/sites
    networks:
      - sastaspace
    depends_on:
      claude-code-api:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8080/"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 15s
    restart: unless-stopped

  frontend:
    build:
      context: ./web
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    env_file:
      - path: .env
        required: false
    environment:
      - NEXT_PUBLIC_BACKEND_URL=http://localhost:8080
    networks:
      - sastaspace
    depends_on:
      backend:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:3000/"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s
    restart: unless-stopped

  claude-code-api:
    build:
      context: ./claude-code-api
      dockerfile: docker/Dockerfile
    ports:
      - "8000:8000"
    env_file:
      - path: .env
        required: false
    environment:
      - WRITE_CLAUDE_CONFIG=true
    volumes:
      - ${HOME}/.claude:/home/claudeuser/.claude:ro
    networks:
      - sastaspace
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 30s
    restart: unless-stopped

  tests:
    build:
      context: ./web
      dockerfile: Dockerfile.test
    profiles:
      - test
    environment:
      - BASE_URL=http://frontend:3000
    networks:
      - sastaspace
    depends_on:
      frontend:
        condition: service_healthy

networks:
  sastaspace:
    driver: bridge

volumes:
  sites_data:
