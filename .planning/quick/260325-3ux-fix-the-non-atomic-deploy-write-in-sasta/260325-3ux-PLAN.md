---
phase: quick
plan: 260325-3ux
type: execute
wave: 1
depends_on: []
files_modified:
  - sastaspace/deployer.py
  - tests/test_deployer.py
autonomous: true
requirements: [quick-fix]
must_haves:
  truths:
    - "index.html writes use atomic temp-file + os.replace pattern"
    - "metadata.json writes use atomic temp-file + os.replace pattern"
    - "No .tmp files left behind after successful deploy"
    - "Existing tests still pass, new test validates atomicity"
  artifacts:
    - path: "sastaspace/deployer.py"
      provides: "Atomic file writes for index.html and metadata.json"
      contains: "os.replace"
    - path: "tests/test_deployer.py"
      provides: "Test coverage for atomic write behavior"
  key_links:
    - from: "sastaspace/deployer.py"
      to: "tempfile or .tmp suffix"
      via: "write-then-rename pattern"
      pattern: "os\\.replace"
---

<objective>
Make all file writes in deployer.py atomic using the write-to-temp + os.replace pattern.

Purpose: A crash mid-write currently leaves truncated index.html or metadata.json while the registry shows status: deployed. The registry write (save_registry) already uses atomic os.replace -- the HTML and metadata writes need the same treatment.
Output: Atomic writes for index.html and metadata.json, plus a test confirming no temp files remain.
</objective>

<execution_context>
@/Users/mkhare/Development/sastaspace/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mkhare/Development/sastaspace/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@sastaspace/deployer.py
@tests/test_deployer.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make index.html and metadata.json writes atomic</name>
  <files>sastaspace/deployer.py, tests/test_deployer.py</files>
  <behavior>
    - Test: After deploy(), no .tmp files exist in the site directory (index.html.tmp, metadata.json.tmp)
    - Test: index.html content is correct after atomic write
    - Test: metadata.json content is correct after atomic write
    - Existing tests continue to pass unchanged
  </behavior>
  <action>
In sastaspace/deployer.py, create a helper function `_atomic_write(path: Path, content: str, encoding: str = "utf-8") -> None` that:
1. Writes content to `{path}.tmp` in the same directory
2. Calls `os.replace(tmp_path, path)` to atomically swap

Apply this helper in the `deploy()` function at two locations:
- Line 146: Replace `(site_dir / "index.html").write_text(html, encoding="utf-8")` with `_atomic_write(site_dir / "index.html", html)`
- Line 153: Replace `(site_dir / "metadata.json").write_text(json.dumps(metadata, indent=2))` with `_atomic_write(site_dir / "metadata.json", json.dumps(metadata, indent=2))`

In tests/test_deployer.py, add a test `test_deploy_no_tmp_files_in_site_dir` that:
1. Calls deploy() normally
2. Lists all files in the site directory
3. Asserts no file ends with `.tmp`

Note: save_registry already uses atomic writes (line 60-64) -- do NOT change that function. The new helper follows the exact same pattern for consistency.
  </action>
  <verify>
    <automated>cd /Users/mkhare/Development/sastaspace/.claude/worktrees/stupefied-dirac && uv run pytest tests/test_deployer.py -x -v</automated>
  </verify>
  <done>All deploy file writes use atomic temp+rename pattern. No .tmp files left after deploy. All existing and new tests pass.</done>
</task>

</tasks>

<verification>
uv run pytest tests/test_deployer.py -x -v
uv run ruff check sastaspace/deployer.py tests/test_deployer.py
</verification>

<success_criteria>
- deployer.py uses atomic write pattern (write-to-tmp + os.replace) for index.html and metadata.json
- No .tmp files left behind after successful deploy
- All existing tests pass, new atomicity test passes
- Ruff linting passes
</success_criteria>

<output>
After completion, create `.planning/quick/260325-3ux-fix-the-non-atomic-deploy-write-in-sasta/260325-3ux-SUMMARY.md`
</output>
