---
phase: 09-premium-ui-redesign
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/app/layout.tsx
  - web/src/app/globals.css
  - web/src/components/backgrounds/flickering-grid.tsx
  - web/src/components/backgrounds/spotlight.tsx
autonomous: true
requirements: []

must_haves:
  truths:
    - "Landing page uses Instrument Serif for headlines, not Inter"
    - "Landing page uses Space Grotesk for body text, not Inter"
    - "Background is warm off-white, not pure white (oklch with nonzero chroma)"
    - "Foreground is warm charcoal, not pure black (oklch with nonzero chroma)"
    - "Gold accent color is present in CSS variables"
    - "FlickeringGrid background component exists and renders"
    - "No purple gradients anywhere in the color palette"
  artifacts:
    - path: "web/src/app/layout.tsx"
      provides: "Font imports for Instrument Serif and Space Grotesk"
      contains: "Instrument_Serif"
    - path: "web/src/app/globals.css"
      provides: "Warm color palette with gold accent"
      contains: "accent"
    - path: "web/src/components/backgrounds/flickering-grid.tsx"
      provides: "Premium flickering grid background component"
      contains: "FlickeringGrid"
  key_links:
    - from: "web/src/app/layout.tsx"
      to: "web/src/app/globals.css"
      via: "CSS custom properties consumed by Tailwind theme"
      pattern: "font-heading.*font-sans"
---

<objective>
Replace the generic Inter font + achromatic color palette + Spotlight background with a premium design system: Instrument Serif + Space Grotesk fonts, warm gold/amber color palette, and FlickeringGrid background.

Purpose: Establish the visual foundation that all component redesigns in Plan 02 will build on.
Output: Updated layout.tsx, globals.css, and new FlickeringGrid background component.
</objective>

<execution_context>
@/Users/mkhare/Development/sastaspace/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mkhare/Development/sastaspace/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-premium-ui-redesign/09-CONTEXT.md

IMPORTANT: This project uses Next.js with potentially different APIs from training data.
Before modifying layout.tsx, read `node_modules/next/dist/docs/` for any font import guidance.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install fonts and update layout.tsx</name>
  <files>web/src/app/layout.tsx</files>
  <read_first>
    - web/src/app/layout.tsx
    - Check node_modules/next/dist/docs/ for font documentation if it exists
  </read_first>
  <action>
Replace Inter with two Google Fonts in layout.tsx:

1. Import `Instrument_Serif` (weight 400, italic variant) and `Space_Grotesk` (weights 400, 500, 600, 700) from `next/font/google`.

2. Configure each font with a CSS variable:
   - Instrument_Serif: `variable: "--font-heading"`, subsets: ["latin"]
   - Space_Grotesk: `variable: "--font-sans"`, subsets: ["latin"]

3. Update the body className to apply both font variables:
   ```
   className={`${instrumentSerif.variable} ${spaceGrotesk.variable} font-sans antialiased`}
   ```

4. Keep all existing metadata unchanged.

Do NOT use Inter anywhere. The heading font (Instrument Serif) will be applied via `font-heading` utility class in components. The body font (Space Grotesk) is the default via `font-sans`.
  </action>
  <verify>
    <automated>cd /Users/mkhare/Development/sastaspace/web && npx next build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "Instrument_Serif" web/src/app/layout.tsx
    - grep -q "Space_Grotesk" web/src/app/layout.tsx
    - grep -q "font-heading" web/src/app/layout.tsx
    - grep -q "font-sans" web/src/app/layout.tsx
    - ! grep -q "Inter" web/src/app/layout.tsx (Inter must be removed)
  </acceptance_criteria>
  <done>layout.tsx imports Instrument Serif and Space Grotesk from Google Fonts, assigns CSS variables, Inter is completely removed</done>
</task>

<task type="auto">
  <name>Task 2: Overhaul globals.css with warm color palette</name>
  <files>web/src/app/globals.css</files>
  <read_first>
    - web/src/app/globals.css
  </read_first>
  <action>
Replace the entire achromatic oklch palette (all `oklch(X 0 0)` values with zero chroma) with a warm palette. Keep the oklch color space but add warmth (nonzero chroma + warm hue angles).

**:root (light mode) values to set:**
- `--background: oklch(0.98 0.005 80)` (warm off-white, slight cream tint)
- `--foreground: oklch(0.20 0.01 50)` (warm charcoal, not pure black)
- `--card: oklch(0.97 0.005 80)` (slightly darker warm white)
- `--card-foreground: oklch(0.20 0.01 50)`
- `--popover: oklch(0.98 0.005 80)`
- `--popover-foreground: oklch(0.20 0.01 50)`
- `--primary: oklch(0.20 0.01 50)` (warm charcoal for primary buttons)
- `--primary-foreground: oklch(0.98 0.005 80)`
- `--secondary: oklch(0.95 0.008 80)` (warm light gray)
- `--secondary-foreground: oklch(0.20 0.01 50)`
- `--muted: oklch(0.94 0.008 80)` (warm muted)
- `--muted-foreground: oklch(0.50 0.01 50)` (warm mid-gray)
- `--accent: oklch(0.72 0.12 75)` (warm gold/amber — this is the #C5A253 equivalent in oklch)
- `--accent-foreground: oklch(0.20 0.01 50)`
- `--destructive: oklch(0.577 0.245 27.325)` (keep existing red)
- `--border: oklch(0.90 0.008 80)` (warm border)
- `--input: oklch(0.90 0.008 80)`
- `--ring: oklch(0.72 0.12 75)` (gold ring to match accent)
- Keep chart values, radius, and sidebar values but make sidebar warm too

**.dark values:** Update similarly — warm charcoal backgrounds instead of pure blacks:
- `--background: oklch(0.16 0.01 50)` (warm dark)
- `--foreground: oklch(0.95 0.005 80)` (warm off-white text)
- `--card: oklch(0.20 0.01 50)`
- `--accent: oklch(0.72 0.12 75)` (same gold)
- `--border: oklch(0.95 0.005 80 / 10%)` (warm white border)
- Update remaining dark values with warm undertones

**Also update the @theme inline block:**
- Add `--font-heading: var(--font-heading);` to the theme so Tailwind generates `font-heading` utility
- Keep all existing theme mappings

**In the @layer base block, keep existing rules unchanged.**

CRITICAL: No purple anywhere. No `oklch(X X 264)` or similar purple hue angles. All hue angles should be in the 50-80 range (warm/amber).
  </action>
  <verify>
    <automated>cd /Users/mkhare/Development/sastaspace/web && npx next build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "0.72 0.12 75" web/src/app/globals.css (gold accent present)
    - grep -q "0.98 0.005 80" web/src/app/globals.css (warm off-white background)
    - grep -q "0.20 0.01 50" web/src/app/globals.css (warm charcoal foreground)
    - grep -q "font-heading" web/src/app/globals.css (heading font variable in theme)
    - ! grep -E "oklch\(0\.\d+ 0 0\)" web/src/app/globals.css (no achromatic values remain, except possibly 0 0 in destructive)
  </acceptance_criteria>
  <done>All CSS variables use warm oklch values with nonzero chroma and warm hue angles. Gold accent at oklch(0.72 0.12 75). No purple. font-heading available as Tailwind utility.</done>
</task>

<task type="auto">
  <name>Task 3: Add FlickeringGrid background component</name>
  <files>web/src/components/backgrounds/flickering-grid.tsx</files>
  <read_first>
    - web/src/components/backgrounds/spotlight.tsx
    - Read the component source from: components/marketing-blocks/backgrounds/dillionverma__flickering-grid.json (extract files[0].content)
  </read_first>
  <action>
Create a new FlickeringGrid component at `web/src/components/backgrounds/flickering-grid.tsx`.

1. Read the JSON file at `components/marketing-blocks/backgrounds/dillionverma__flickering-grid.json` and extract the component source from `files[0].content`.

2. Copy the component source into the new file. Adapt it:
   - Keep the canvas-based flickering grid implementation as-is
   - Default color to warm charcoal: `color = "rgb(120, 100, 70)"` (warm tone matching the palette)
   - Default `maxOpacity = 0.15` (subtle, not competing with content)
   - Default `squareSize = 4`, `gridGap = 6`, `flickerChance = 0.3`
   - Export as named export: `export { FlickeringGrid }`

3. Do NOT delete spotlight.tsx yet — Plan 02 will swap it in the hero. Just create the new file.

The component uses a canvas element with IntersectionObserver for performance (only animates when visible). It accepts className for positioning.
  </action>
  <verify>
    <automated>cd /Users/mkhare/Development/sastaspace/web && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - test -f web/src/components/backgrounds/flickering-grid.tsx
    - grep -q "FlickeringGrid" web/src/components/backgrounds/flickering-grid.tsx
    - grep -q "canvasRef" web/src/components/backgrounds/flickering-grid.tsx
    - grep -q "use client" web/src/components/backgrounds/flickering-grid.tsx
  </acceptance_criteria>
  <done>FlickeringGrid component exists with warm default color, subtle opacity, canvas-based rendering with IntersectionObserver performance optimization</done>
</task>

</tasks>

<verification>
1. `cd web && npx next build` completes without errors
2. `cd web && npx tsc --noEmit` passes with no type errors
3. No Inter font references remain in layout.tsx
4. No achromatic oklch values (oklch with 0 chroma) remain in globals.css (except destructive red)
5. FlickeringGrid component file exists and exports correctly
</verification>

<success_criteria>
- Instrument Serif and Space Grotesk loaded via next/font/google
- All color variables use warm oklch values with gold accent
- FlickeringGrid background component ready for integration
- Build passes cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/09-premium-ui-redesign/09-01-SUMMARY.md`
</output>
