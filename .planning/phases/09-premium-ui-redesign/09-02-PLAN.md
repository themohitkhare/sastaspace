---
phase: 09-premium-ui-redesign
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - web/src/components/landing/hero-section.tsx
  - web/src/components/landing/url-input-form.tsx
  - web/src/components/landing/how-it-works.tsx
  - web/src/components/result/result-view.tsx
  - web/src/components/result/contact-form.tsx
  - web/src/components/progress/progress-view.tsx
  - web/src/components/progress/step-indicator.tsx
autonomous: false

must_haves:
  truths:
    - "Hero section uses asymmetric layout, not centered text + input below"
    - "Hero headline renders in Instrument Serif (font-heading class)"
    - "How-it-works uses vertical editorial layout, not three-column icon grid"
    - "FlickeringGrid replaces Spotlight as background in hero"
    - "Result page has more whitespace and uses new typography"
    - "Contact form uses warm color palette and new fonts"
    - "Progress view uses new typography and warm colors"
    - "Staggered fade-up animations on hero elements"
  artifacts:
    - path: "web/src/components/landing/hero-section.tsx"
      provides: "Asymmetric hero with editorial headline"
      contains: "font-heading"
    - path: "web/src/components/landing/how-it-works.tsx"
      provides: "Vertical editorial how-it-works section"
      contains: "font-heading"
    - path: "web/src/components/result/result-view.tsx"
      provides: "Polished result page with warm typography"
      contains: "font-heading"
    - path: "web/src/components/progress/progress-view.tsx"
      provides: "Updated progress view with new typography"
      contains: "font-heading"
  key_links:
    - from: "web/src/components/landing/hero-section.tsx"
      to: "web/src/components/backgrounds/flickering-grid.tsx"
      via: "import and render FlickeringGrid"
      pattern: "FlickeringGrid"
    - from: "web/src/components/landing/hero-section.tsx"
      to: "web/src/components/landing/url-input-form.tsx"
      via: "UrlInputForm component"
      pattern: "UrlInputForm"
---

<objective>
Redesign all user-facing components to use the new design system from Plan 01. Break the generic AI-template aesthetic with asymmetric layouts, editorial typography, and purposeful animation.

Purpose: Transform every screen from generic template to premium $5k-custom-build feel.
Output: Redesigned hero, how-it-works, result, contact form, and progress components.
</objective>

<execution_context>
@/Users/mkhare/Development/sastaspace/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mkhare/Development/sastaspace/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-premium-ui-redesign/09-CONTEXT.md
@.planning/phases/09-premium-ui-redesign/09-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs -->

From web/src/app/globals.css (after Plan 01):
- `--accent: oklch(0.72 0.12 75)` — gold accent color, use via `text-accent` or `bg-accent`
- `--background: oklch(0.98 0.005 80)` — warm off-white
- `--foreground: oklch(0.20 0.01 50)` — warm charcoal
- `font-heading` Tailwind utility — applies Instrument Serif
- `font-sans` Tailwind utility — applies Space Grotesk (default)

From web/src/components/backgrounds/flickering-grid.tsx (after Plan 01):
```typescript
export { FlickeringGrid };
// Props: squareSize, gridGap, flickerChance, color, width, height, className, maxOpacity
```

From web/src/components/landing/url-input-form.tsx (unchanged):
```typescript
interface UrlInputFormProps {
  onSubmit: (url: string) => void;
}
export function UrlInputForm({ onSubmit }: UrlInputFormProps)
```

From web/src/components/landing/hero-section.tsx (unchanged interface):
```typescript
interface HeroSectionProps {
  onSubmit: (url: string) => void;
}
export function HeroSection({ onSubmit }: HeroSectionProps)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Redesign hero-section.tsx with asymmetric layout</name>
  <files>web/src/components/landing/hero-section.tsx, web/src/components/landing/url-input-form.tsx</files>
  <read_first>
    - web/src/components/landing/hero-section.tsx
    - web/src/components/landing/url-input-form.tsx
    - web/src/components/backgrounds/flickering-grid.tsx
    - web/src/components/app-flow.tsx (to understand how HeroSection is consumed)
  </read_first>
  <action>
**hero-section.tsx — Complete rewrite, keep same interface (HeroSectionProps with onSubmit):**

1. Replace Spotlight import with FlickeringGrid: `import { FlickeringGrid } from "@/components/backgrounds/flickering-grid"`

2. Layout structure — break the centered symmetry:
   ```
   <div className="relative min-h-screen flex flex-col justify-center overflow-hidden bg-background">
     <FlickeringGrid className="absolute inset-0 z-0" />
     <div className="relative z-10 w-full max-w-6xl mx-auto px-6 sm:px-8 lg:px-12 py-24">
       {/* Left-aligned content, NOT centered */}
       <div className="max-w-2xl">
         {headline}
         {subtitle}
         {form}
       </div>
     </div>
   </div>
   ```

3. Headline — editorial feel with Instrument Serif:
   - Use `font-heading` class on h1
   - Size: `text-[clamp(2.5rem,6vw,4.5rem)]` for fluid scaling
   - Line height: `leading-[1.08]` (tight, editorial)
   - Text: "See your website reimagined" — split across lines with a `<br className="hidden sm:block" />` after "website" for deliberate line breaking
   - Add a gold accent on one word: wrap "reimagined" in `<span className="text-accent">reimagined</span>`

4. Subtitle:
   - `text-lg sm:text-xl text-muted-foreground font-sans`
   - `mt-6 mb-10 max-w-lg`
   - Body text at 18px+ per design decisions

5. Staggered fade-up animations using motion/react:
   - Container: `initial={{ opacity: 0 }} animate={{ opacity: 1 }}`
   - Headline: `initial={{ opacity: 0, y: 16 }} animate={{ opacity: 1, y: 0 }} transition={{ duration: 0.4, delay: 0.1 }}`
   - Subtitle: same but `delay: 0.2`
   - Form: same but `delay: 0.3`
   - Stagger of ~100ms between elements (0.1, 0.2, 0.3)
   - y offset of 16px (within the 8-16px range from decisions)

6. Keep the existing `HeroSectionProps` interface and `onSubmit` prop exactly as-is.

**url-input-form.tsx — Style updates only, keep all logic:**

- Update the submit button to use the gold accent: `className="h-12 min-h-12 rounded-lg sm:rounded-l-none px-8 bg-accent text-accent-foreground hover:bg-accent/90 font-medium"`
- Make the input taller: `h-12` (already is) with `text-base`
- Add `font-sans` explicitly to the input for consistency
- Keep ALL existing validation logic, favicon fetching, error display unchanged
  </action>
  <verify>
    <automated>cd /Users/mkhare/Development/sastaspace/web && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "FlickeringGrid" web/src/components/landing/hero-section.tsx
    - grep -q "font-heading" web/src/components/landing/hero-section.tsx
    - grep -q "text-accent" web/src/components/landing/hero-section.tsx
    - grep -q "clamp" web/src/components/landing/hero-section.tsx
    - ! grep -q "Spotlight" web/src/components/landing/hero-section.tsx
    - ! grep -q "items-center text-center" web/src/components/landing/hero-section.tsx (not centered)
    - grep -q "bg-accent" web/src/components/landing/url-input-form.tsx
  </acceptance_criteria>
  <done>Hero has left-aligned asymmetric layout, Instrument Serif headline with gold accent on "reimagined", FlickeringGrid background, staggered fade-up animations at 100ms intervals, fluid clamp() sizing. URL input button uses gold accent.</done>
</task>

<task type="auto">
  <name>Task 2: Redesign how-it-works.tsx with vertical editorial layout</name>
  <files>web/src/components/landing/how-it-works.tsx</files>
  <read_first>
    - web/src/components/landing/how-it-works.tsx
    - web/src/components/app-flow.tsx (to see how HowItWorks is consumed)
  </read_first>
  <action>
**Complete rewrite — break the three-column icon grid pattern.**

Replace with a vertical editorial layout with numbered steps. Keep the same export name `HowItWorks` and same data (3 steps).

1. Add "use client" and import motion from "motion/react" for fade-up animations.

2. Section wrapper:
   - `<section className="w-full max-w-6xl mx-auto px-6 sm:px-8 lg:px-12 py-20">`
   - NOT inside a bg-muted rounded box — let it breathe

3. Section heading:
   - `<h2 className="font-heading text-[clamp(1.75rem,4vw,2.5rem)] leading-[1.1] text-foreground mb-16">`
   - Text: "How it works" — left-aligned, not centered

4. Steps as vertical editorial blocks with large step numbers:
   ```
   {steps.map((step, i) => (
     <motion.div
       key={step.number}
       initial={{ opacity: 0, y: 12 }}
       whileInView={{ opacity: 1, y: 0 }}
       viewport={{ once: true, margin: "-50px" }}
       transition={{ duration: 0.4, delay: i * 0.1 }}
       className="flex gap-8 sm:gap-12 py-10 border-t border-border"
     >
       <span className="font-heading text-[3rem] sm:text-[4rem] leading-none text-accent/40 shrink-0">
         {step.number}
       </span>
       <div className="pt-2">
         <h3 className="text-xl sm:text-2xl font-medium text-foreground mb-2">{step.label}</h3>
         <p className="text-base text-muted-foreground max-w-md">{step.description}</p>
       </div>
     </motion.div>
   ))}
   ```

5. Each step is a horizontal row: large faded gold number on the left, title + description on the right. Separated by `border-t border-border`. This creates a vertical editorial flow instead of the equal-column grid.

6. Keep the same step data array (3 steps with number, label, description).

7. Update the `<section>` wrapper in app-flow.tsx: change `<section className="py-12 px-4">` to `<section className="py-0">` since the how-it-works component now handles its own padding. Actually, the simpler approach: leave app-flow.tsx alone since max-w-6xl + px handling is inside how-it-works.tsx itself. Just remove the redundant padding if needed — read app-flow.tsx first.
  </action>
  <verify>
    <automated>cd /Users/mkhare/Development/sastaspace/web && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "font-heading" web/src/components/landing/how-it-works.tsx
    - grep -q "border-t" web/src/components/landing/how-it-works.tsx (vertical separator)
    - grep -q "text-accent" web/src/components/landing/how-it-works.tsx (gold numbers)
    - grep -q "whileInView" web/src/components/landing/how-it-works.tsx (scroll-triggered animation)
    - ! grep -q "sm:flex-row" web/src/components/landing/how-it-works.tsx (no three-column layout)
  </acceptance_criteria>
  <done>How-it-works uses vertical editorial layout with large gold step numbers, border separators, scroll-triggered fade-up animations. No three-column grid.</done>
</task>

<task type="auto">
  <name>Task 3: Polish result-view.tsx and contact-form.tsx</name>
  <files>web/src/components/result/result-view.tsx, web/src/components/result/contact-form.tsx</files>
  <read_first>
    - web/src/components/result/result-view.tsx
    - web/src/components/result/contact-form.tsx
  </read_first>
  <action>
**result-view.tsx — Typography and spacing polish, keep all logic:**

1. Update h1 headline:
   - Add `font-heading` class
   - Size: `text-[clamp(1.75rem,5vw,3rem)]`
   - Keep leading-[1.1]
   - Increase top padding: `pt-24` instead of `pt-16`

2. Increase max-width for more breathing room: `max-w-4xl` instead of `max-w-3xl`

3. Update the "Take me to the future" CTA button:
   - `bg-accent text-accent-foreground hover:bg-accent/90` (gold button)
   - `text-base font-medium h-12 px-8` (slightly larger)

4. Update the "View original site" link:
   - Keep muted styling as-is, it's already good

5. Add more spacing before contact form: `mt-16` instead of `mt-12`

6. Keep ALL existing logic (subdomain parsing, iframe src, isShareable) unchanged.

**contact-form.tsx — Typography and color polish, keep all logic:**

1. Update h2 heading:
   - Add `font-heading` class
   - Size: `text-[clamp(1.5rem,3vw,2rem)]`
   - Keep existing text content

2. Update submit button:
   - `bg-accent text-accent-foreground hover:bg-accent/90` (gold, consistent with hero CTA)
   - Keep the Loader2 spinner and "Sending..." state

3. Update the success state h2:
   - Add `font-heading` class

4. Keep ALL existing validation logic, Turnstile integration, honeypot, form state management completely unchanged. Only modify className strings and add font-heading to headings.
  </action>
  <verify>
    <automated>cd /Users/mkhare/Development/sastaspace/web && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "font-heading" web/src/components/result/result-view.tsx
    - grep -q "font-heading" web/src/components/result/contact-form.tsx
    - grep -q "bg-accent" web/src/components/result/result-view.tsx (gold CTA)
    - grep -q "bg-accent" web/src/components/result/contact-form.tsx (gold submit)
    - grep -q "clamp" web/src/components/result/result-view.tsx (fluid sizing)
  </acceptance_criteria>
  <done>Result page and contact form use Instrument Serif headings, gold accent buttons, more whitespace, fluid typography. All business logic unchanged.</done>
</task>

<task type="auto">
  <name>Task 4: Update progress-view.tsx with new typography</name>
  <files>web/src/components/progress/progress-view.tsx, web/src/components/progress/step-indicator.tsx</files>
  <read_first>
    - web/src/components/progress/progress-view.tsx
    - web/src/components/progress/step-indicator.tsx
  </read_first>
  <action>
**progress-view.tsx — Typography updates, keep all logic:**

1. Error state h2 ("Something went wrong"):
   - Add `font-heading` class
   - Keep text-2xl, or upgrade to `text-[clamp(1.5rem,3vw,2rem)]`

2. Error state retry button:
   - Add `bg-accent text-accent-foreground hover:bg-accent/90` for gold accent consistency

3. Status label (the animated text showing current step):
   - `text-lg font-medium` instead of `text-base`
   - Add `font-heading` for the main status text

4. Keep ALL state logic, step labels, connecting/progress/error handling unchanged.

**step-indicator.tsx — Subtle color updates:**

1. When status is "done", the Check icon should use `text-accent` instead of `text-primary` (gold checkmark instead of dark)

2. The Progress bar: if using a custom Progress component, consider if it respects the accent color. The existing Progress component likely uses `bg-primary` — this is fine as `--primary` is now warm charcoal. If the bar color looks wrong, add a note for the human verify checkpoint.

3. Keep ALL existing props and logic unchanged.
  </action>
  <verify>
    <automated>cd /Users/mkhare/Development/sastaspace/web && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "font-heading" web/src/components/progress/progress-view.tsx
    - grep -q "bg-accent" web/src/components/progress/progress-view.tsx (gold retry button)
    - grep -q "text-accent" web/src/components/progress/step-indicator.tsx (gold checkmark)
  </acceptance_criteria>
  <done>Progress view and step indicator use Instrument Serif for headings, gold accent for interactive elements and completion states. All state management unchanged.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: Visual verification of redesigned UI</name>
  <files>None — verification only</files>
  <action>Human visually verifies the complete UI redesign across all pages.</action>
  <what-built>Complete visual overhaul of all pages: landing (hero + how-it-works), progress, and result (preview + contact form). New typography (Instrument Serif + Space Grotesk), warm gold/amber color palette, asymmetric hero layout, vertical editorial how-it-works, FlickeringGrid background.</what-built>
  <how-to-verify>
    1. Run `cd web && npm run dev` to start the dev server
    2. Visit http://localhost:3000
    3. Verify landing page:
       - Hero headline is in a serif font (Instrument Serif), left-aligned, NOT centered
       - "reimagined" word has gold/amber color accent
       - Background has a subtle flickering grid pattern (small squares flickering)
       - Body text is in Space Grotesk (clean sans-serif, NOT Inter)
       - Page background is warm off-white (slight cream), NOT pure white
       - CTA button is gold/amber colored
    4. Scroll down to How It Works:
       - Layout is vertical (stacked rows), NOT three equal columns
       - Large faded gold numbers (1, 2, 3) on the left
       - Rows separated by horizontal lines
    5. Submit a URL to test progress view:
       - Status text uses serif font
       - Retry button (if error) is gold
    6. Check a result page (e.g., /example-com/):
       - Headline in serif font
       - "Take me to the future" button is gold
       - Contact form heading in serif font
       - Submit button is gold
    7. Verify NO purple gradients, NO Inter font, NO three-column grids
  </how-to-verify>
  <verify>
    <automated>cd /Users/mkhare/Development/sastaspace/web && npx next build 2>&1 | tail -3</automated>
  </verify>
  <done>Human approves that the redesign looks premium — no AI-template aesthetic remains</done>
  <resume-signal>Type "approved" if the redesign looks premium, or describe specific issues to fix</resume-signal>
</task>

</tasks>

<verification>
1. `cd web && npx next build` completes without errors
2. `cd web && npx tsc --noEmit` passes
3. All headings use `font-heading` (Instrument Serif)
4. All CTA buttons use `bg-accent` (gold)
5. Hero is left-aligned with FlickeringGrid background
6. How-it-works is vertical editorial, not three-column
7. No Spotlight import remains in hero
8. Human approves visual result
</verification>

<success_criteria>
- Every heading across all pages renders in Instrument Serif
- Every CTA button uses the warm gold accent color
- Hero layout is asymmetric (left-aligned), not centered
- How-it-works is vertical editorial with large step numbers
- FlickeringGrid background replaces Spotlight in hero
- All animations are purposeful fade-ups with staggered timing
- Human verifies the result looks like a $5k custom build, not an AI template
</success_criteria>

<output>
After completion, create `.planning/phases/09-premium-ui-redesign/09-02-SUMMARY.md`
</output>
