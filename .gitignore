# Dependencies
node_modules
.pnpm-store

# Build outputs
.next
out
dist
build
target
coverage
.coverage
.pytest_cache
__pycache__
.venv
*.wasm
lcov.info

# SpacetimeDB local data
.spacetime
spacetime-data

# Generated bindings are tracked — they're code, they unblock fresh clones,
# and CI regenerates+commits them. Keeping the per-package .gitignore in case
# we ever want to re-flip this.

# Env / secrets
.env
.env.local
.env.*.local

# OS / editor
.DS_Store
.idea
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json

# Logs
*.log
npm-debug.log*
pnpm-debug.log*

# Playwright MCP test artifacts
.playwright-mcp/

# Cloudflare
.wrangler
*.egg-info/
*.tsbuildinfo
*.pyc
graphify-out/
.claude/

# Rust (additional)
**/*.rs.bk
Cargo.lock.bak

# Insta pending snapshots
**/*.snap.new