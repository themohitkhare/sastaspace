{
  "$schema": "https://ui.shadcn.com/schema/registry-item.json",
  "name": "warp-tunnel",
  "type": "registry:component",
  "title": "Warp Tunnel",
  "description": "A warp-tunnel component.",
  "dependencies": [],
  "registryDependencies": [],
  "files": [
    {
      "path": "src/components/ui/warp-tunnel.tsx",
      "content": "\"use client\";\n\nimport React, {\n  FC,\n  useRef,\n  useEffect,\n  useState,\n} from \"react\";\n\nexport interface WarpTunnelProps {\n  /** Number of mirrored segments (default: 8) */\n  segments?: number;\n  /** Speed multiplier for the animation (default: 1.5) */\n  speed?: number;\n  /** Additional CSS classes on the wrapper */\n  className?: string;\n  /** Accessible label for screen readers */\n  ariaLabel?: string;\n}\n\nconst vertexShaderSource = `#version 300 es\nin vec2 a_position;\nvoid main() {\n  gl_Position = vec4(a_position, 0.0, 1.0);\n}\n`;\n\nconst fragmentShaderSource = `#version 300 es\nprecision highp float;\n\nuniform float u_time;\nuniform vec2  u_resolution;\nuniform float u_segments;\nuniform float u_speed;\n\n// Inigo Quilez palette\nvec3 palette(float t, vec3 a, vec3 b, vec3 c, vec3 d) {\n  return a + b * cos(6.28318 * (c * t + d));\n}\n\nout vec4 fragColor;\n\nvoid main() {\n  // normalize coordinates\n  vec2 uv = (gl_FragCoord.xy - 0.5 * u_resolution.xy)\n            / u_resolution.y;\n\n  // polar coords\n  float angle  = atan(uv.y, uv.x);\n  float radius = length(uv);\n\n  // tunnel coordinate\n  float tc = 1.0 / max(radius, 0.0001);\n\n  // swirl + time\n  float pattern = tc + angle * u_segments\n                  + u_time * u_speed;\n\n  // color lookup\n  vec3 color = palette(\n    pattern * 0.1 + u_time * 0.1,\n    vec3(0.5),\n    vec3(0.5),\n    vec3(1.0),\n    vec3(0.0, 0.1, 0.2)\n  );\n\n  // fade edges\n  color *= (1.0 - smoothstep(0.0, 2.0, radius));\n\n  fragColor = vec4(color, 1.0);\n}\n`;\n\nconst WarpTunnel: FC<WarpTunnelProps> = ({\n  segments  = 8,\n  speed     = 1.5,\n  className = \"\",\n  ariaLabel = \"Warp tunnel shader background\",\n}) => {\n  const canvasRef = useRef<HTMLCanvasElement>(null);\n  const [error, setError] = useState<string | null>(null);\n  const frameId = useRef<number>();\n\n  useEffect(() => {\n    const canvas = canvasRef.current;\n    if (!canvas) return;\n\n    // Try WebGL2 first for #version 300 es\n    const gl =\n      (canvas.getContext(\"webgl2\") as WebGL2RenderingContext)\n      || (canvas.getContext(\"webgl\")   as WebGLRenderingContext);\n    if (!gl) {\n      setError(\"WebGL not supported\");\n      return;\n    }\n\n    // Shader compile helper\n    function compileShader(\n      type: GLenum,\n      src: string\n    ): WebGLShader | null {\n      const sh = gl.createShader(type)!;\n      gl.shaderSource(sh, src);\n      gl.compileShader(sh);\n      if (!gl.getShaderParameter(sh, gl.COMPILE_STATUS)) {\n        console.error(gl.getShaderInfoLog(sh));\n        gl.deleteShader(sh);\n        setError(\"Shader compile error (see console)\");\n        return null;\n      }\n      return sh;\n    }\n\n    // Compile shaders\n    const vs = compileShader(gl.VERTEX_SHADER,   vertexShaderSource);\n    const fs = compileShader(gl.FRAGMENT_SHADER, fragmentShaderSource);\n    if (!vs || !fs) return;\n\n    // Link program\n    const prog = gl.createProgram()!;\n    gl.attachShader(prog, vs);\n    gl.attachShader(prog, fs);\n    gl.linkProgram(prog);\n    if (!gl.getProgramParameter(prog, gl.LINK_STATUS)) {\n      console.error(gl.getProgramInfoLog(prog));\n      setError(\"Program link error (see console)\");\n      return;\n    }\n\n    // Look up locations\n    const posLoc     = gl.getAttribLocation(prog, \"a_position\");\n    const resLoc     = gl.getUniformLocation(prog, \"u_resolution\")!;\n    const timeLoc    = gl.getUniformLocation(prog, \"u_time\")!;\n    const segLoc     = gl.getUniformLocation(prog, \"u_segments\")!;\n    const speedLoc   = gl.getUniformLocation(prog, \"u_speed\")!;\n\n    // Full-screen quad\n    const quad = new Float32Array([\n      -1,  1,\n      -1, -1,\n       1,  1,\n       1, -1,\n    ]);\n    const buf = gl.createBuffer()!;\n    gl.bindBuffer(gl.ARRAY_BUFFER, buf);\n    gl.bufferData(gl.ARRAY_BUFFER, quad, gl.STATIC_DRAW);\n\n    // Resize handler\n    const resize = () => {\n      const dpr = window.devicePixelRatio || 1;\n      canvas.width  = canvas.clientWidth  * dpr;\n      canvas.height = canvas.clientHeight * dpr;\n      gl.viewport(0, 0, canvas.width, canvas.height);\n    };\n    window.addEventListener(\"resize\", resize);\n    resize();\n\n    // Render loop\n    const render = (t: number) => {\n      if (error) return;\n\n      gl.clear(gl.COLOR_BUFFER_BIT);\n      gl.useProgram(prog);\n\n      // Bind attributes\n      gl.enableVertexAttribArray(posLoc);\n      gl.bindBuffer(gl.ARRAY_BUFFER, buf);\n      gl.vertexAttribPointer(posLoc, 2, gl.FLOAT, false, 0, 0);\n\n      // Set uniforms\n      gl.uniform2f(resLoc, canvas.width, canvas.height);\n      gl.uniform1f(timeLoc, t * 0.001);\n      gl.uniform1f(segLoc, segments);\n      gl.uniform1f(speedLoc, speed);\n\n      gl.drawArrays(gl.TRIANGLE_STRIP, 0, 4);\n      frameId.current = requestAnimationFrame(render);\n    };\n    frameId.current = requestAnimationFrame(render);\n\n    // Cleanup\n    return () => {\n      cancelAnimationFrame(frameId.current!);\n      window.removeEventListener(\"resize\", resize);\n    };\n  }, [segments, speed, error]);\n\n  return (\n    <div\n      role=\"region\"\n      aria-label={ariaLabel}\n      className={`relative w-full h-full overflow-hidden ${className}`}\n    >\n      {error && (\n        <div className=\"absolute inset-0 bg-black/80 flex items-center justify-center text-white font-mono p-4\">\n          {error}\n        </div>\n      )}\n      <canvas\n        ref={canvasRef}\n        className=\"block w-full h-full\"\n      />\n    </div>\n  );\n};\n\nexport default WarpTunnel;\n",
      "type": "registry:component"
    }
  ],
  "_meta": {
    "author": "dhileepkumargm",
    "slug": "warp-tunnel",
    "registry_url": "https://21st.dev/r/dhileepkumargm/warp-tunnel",
    "title": "Warp Tunnel",
    "description": "A React UI component that renders an infinite, color-shifting warp-speed tunnel using WebGL2 and GLSL shaders.",
    "category": "shader",
    "category_group": "Marketing Blocks",
    "downloads": 9,
    "likes": 0
  }
}