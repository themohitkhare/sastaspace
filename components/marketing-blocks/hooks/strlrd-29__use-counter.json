{
  "name": "use-counter",
  "type": "registry:hook",
  "registryDependencies": [],
  "files": [
    {
      "path": "/components/hooks/use-counter.tsx",
      "content": "\"use client\"\n\nimport * as React from \"react\"\n\ntype UseCounterReturn = {\n  count: number\n  increment: () => void\n  decrement: () => void\n  reset: () => void\n  setCount: React.Dispatch<React.SetStateAction<number>>\n}\n\nfunction useCounter(initialValue?: number): UseCounterReturn {\n  const [count, setCount] = React.useState(initialValue ?? 0)\n\n  const increment = React.useCallback(() => {\n    setCount((x) => x + 1)\n  }, [])\n\n  const decrement = React.useCallback(() => {\n    setCount((x) => x - 1)\n  }, [])\n\n  const reset = React.useCallback(() => {\n    setCount(initialValue ?? 0)\n  }, [initialValue])\n\n  return {\n    count,\n    increment,\n    decrement,\n    reset,\n    setCount,\n  }\n}\n\nexport { useCounter }",
      "type": "registry:hook",
      "target": ""
    },
    {
      "path": "/hooks/use-counter.tsx",
      "content": "\"use client\"\n\nimport * as React from \"react\"\n\ntype UseCounterReturn = {\n  count: number\n  increment: () => void\n  decrement: () => void\n  reset: () => void\n  setCount: React.Dispatch<React.SetStateAction<number>>\n}\n\nfunction useCounter(initialValue?: number): UseCounterReturn {\n  const [count, setCount] = React.useState(initialValue ?? 0)\n\n  const increment = React.useCallback(() => {\n    setCount((x) => x + 1)\n  }, [])\n\n  const decrement = React.useCallback(() => {\n    setCount((x) => x - 1)\n  }, [])\n\n  const reset = React.useCallback(() => {\n    setCount(initialValue ?? 0)\n  }, [initialValue])\n\n  return {\n    count,\n    increment,\n    decrement,\n    reset,\n    setCount,\n  }\n}\n\nexport { useCounter }",
      "type": "registry:hook",
      "target": ""
    }
  ],
  "tailwind": {
    "config": {}
  },
  "_meta": {
    "author": "strlrd-29",
    "slug": "use-counter",
    "registry_url": "https://21st.dev/r/strlrd-29/use-counter",
    "title": "useCounter",
    "description": "Manage a numeric state with useCounter, offering functions to increment, decrement, reset, and set a custom value.",
    "category": "hook",
    "category_group": "Marketing Blocks",
    "downloads": 37,
    "likes": 0
  }
}