{
  "$schema": "https://ui.shadcn.com/schema/registry-item.json",
  "name": "kaleidoscopic-shader",
  "type": "registry:component",
  "title": "Kaleidoscopic Shader",
  "description": "A kaleidoscopic-shader component.",
  "dependencies": [],
  "registryDependencies": [],
  "files": [
    {
      "path": "src/components/ui/kaleidoscopic-shader.tsx",
      "content": "// src/components/ui/kaleidoscopic-shader.tsx\n\"use client\";\n\nimport React, { FC, useRef, useEffect, useState } from \"react\";\n\nexport interface KaleidoscopicTunnelProps {\n  /** Number of mirrored segments */\n  segments?: number;\n  /** Animation speed multiplier */\n  speed?: number;\n  /** Extra wrapper CSS classes */\n  className?: string;\n  /** ARIA label for screen readers */\n  ariaLabel?: string;\n}\n\nexport const kaleidoscopicTunnelShader = `#version 300 es\nprecision highp float;\n\nuniform float u_time;\nuniform vec2  u_resolution;\nuniform float u_segments;\nuniform float u_speed;\nout vec4 fragColor;\n\n// High‐quality palette by Inigo Quilez\nvec3 palette(float t, vec3 a, vec3 b, vec3 c, vec3 d) {\n  return a + b * cos(6.28318 * (c * t + d));\n}\n\nvoid main() {\n  vec2 uv = (gl_FragCoord.xy - 0.5 * u_resolution.xy) / u_resolution.y;\n  float angle  = atan(uv.y, uv.x);\n  float radius = length(uv);\n\n  float tc = 1.0 / max(radius, 0.0001);\n  angle = fract(angle * u_segments / (2.0 * 3.14159));\n\n  float pattern = tc + angle * 10.0 - u_time * u_speed;\n  vec3 color = palette(\n    pattern * 0.1,\n    vec3(0.5), vec3(0.5), vec3(1.0),\n    vec3(0.0, 0.33, 0.67)\n  );\n\n  color *= (1.0 - smoothstep(0.0, 1.5, radius));\n  color += smoothstep(0.05, 0.0, radius);\n\n  fragColor = vec4(color, 1.0);\n}\n`;\n\nconst KaleidoscopicTunnel: FC<KaleidoscopicTunnelProps> = ({\n  segments  = 8,\n  speed     = 2,\n  className = \"\",\n  ariaLabel = \"Kaleidoscopic tunnel shader background\",\n}) => {\n  const canvasRef = useRef<HTMLCanvasElement>(null);\n  const [error, setError] = useState<string|null>(null);\n\n  useEffect(() => {\n    const canvas = canvasRef.current;\n    if (!canvas) return;\n\n    const gl = canvas.getContext(\"webgl2\");\n    if (!gl) {\n      setError(\"WebGL2 not supported in this browser.\");\n      return;\n    }\n\n    // Helper: compile a WebGL2 shader\n    const compileShader = (type: GLenum, src: string) => {\n      const sh = gl.createShader(type)!;\n      gl.shaderSource(sh, src);\n      gl.compileShader(sh);\n      if (!gl.getShaderParameter(sh, gl.COMPILE_STATUS)) {\n        console.error(\"Shader compile error:\", gl.getShaderInfoLog(sh));\n        gl.deleteShader(sh);\n        setError(\"Shader compile error. See console.\");\n        return null;\n      }\n      return sh;\n    };\n\n    // Vertex + fragment\n    const vsSrc = `#version 300 es\n    in vec2 a_position;\n    void main() {\n      gl_Position = vec4(a_position, 0.0, 1.0);\n    }`;\n    const vs = compileShader(gl.VERTEX_SHADER, vsSrc);\n    const fs = compileShader(gl.FRAGMENT_SHADER, kaleidoscopicTunnelShader);\n    if (!vs || !fs) return;\n\n    // Link program\n    const prog = gl.createProgram()!;\n    gl.attachShader(prog, vs);\n    gl.attachShader(prog, fs);\n    gl.linkProgram(prog);\n    if (!gl.getProgramParameter(prog, gl.LINK_STATUS)) {\n      console.error(\"Program link error:\", gl.getProgramInfoLog(prog));\n      setError(\"Program link error. See console.\");\n      return;\n    }\n\n    // Look up locations\n    const posLoc  = gl.getAttribLocation(prog, \"a_position\");\n    const resLoc  = gl.getUniformLocation(prog, \"u_resolution\")!;\n    const timeLoc = gl.getUniformLocation(prog, \"u_time\")!;\n    const segLoc  = gl.getUniformLocation(prog, \"u_segments\")!;\n    const speedLoc= gl.getUniformLocation(prog, \"u_speed\")!;\n\n    // Full‐screen quad\n    const quad = new Float32Array([-1, 1, -1, -1, 1, 1, 1, -1]);\n    const buf  = gl.createBuffer()!;\n    gl.bindBuffer(gl.ARRAY_BUFFER, buf);\n    gl.bufferData(gl.ARRAY_BUFFER, quad, gl.STATIC_DRAW);\n\n    // Handle resize\n    const resize = () => {\n      const dpr = window.devicePixelRatio || 1;\n      canvas.width  = canvas.clientWidth  * dpr;\n      canvas.height = canvas.clientHeight * dpr;\n      gl.viewport(0, 0, canvas.width, canvas.height);\n    };\n    window.addEventListener(\"resize\", resize);\n    resize();\n\n    // Render loop\n    let frameId: number;\n    const render = (t: number) => {\n      if (error) return;\n\n      gl.clearColor(0, 0, 0, 1);\n      gl.clear(gl.COLOR_BUFFER_BIT);\n      gl.useProgram(prog);\n\n      // Bind attributes + uniforms\n      gl.enableVertexAttribArray(posLoc);\n      gl.bindBuffer(gl.ARRAY_BUFFER, buf);\n      gl.vertexAttribPointer(posLoc, 2, gl.FLOAT, false, 0, 0);\n\n      gl.uniform2f(resLoc, canvas.width, canvas.height);\n      gl.uniform1f(timeLoc, t * 0.001);\n      gl.uniform1f(segLoc, segments);\n      gl.uniform1f(speedLoc, speed);\n\n      gl.drawArrays(gl.TRIANGLE_STRIP, 0, 4);\n      frameId = requestAnimationFrame(render);\n    };\n    frameId = requestAnimationFrame(render);\n\n    return () => {\n      window.removeEventListener(\"resize\", resize);\n      cancelAnimationFrame(frameId);\n    };\n  }, [segments, speed, error]);\n\n  return (\n    <div\n      role=\"region\"\n      aria-label={ariaLabel}\n      className={`relative w-full h-full overflow-hidden ${className}`}\n    >\n      <canvas ref={canvasRef} className=\"block w-full h-full\" />\n      {error && (\n        <div className=\"absolute inset-0 bg-black/80 flex items-center justify-center text-white p-4 font-mono text-sm\">\n          {error}\n        </div>\n      )}\n    </div>\n  );\n};\n\nexport default KaleidoscopicTunnel;\n",
      "type": "registry:component"
    }
  ],
  "_meta": {
    "author": "dhileepkumargm",
    "slug": "kaleidoscopic-shader",
    "registry_url": "https://21st.dev/r/dhileepkumargm/kaleidoscopic-shader",
    "title": "Kaleidoscopic  shader",
    "description": "polar coordinates to create the repeating segments and implemented a high-quality color palette function, as described by Inigo Quilez, to produce the smooth, psychedelic color transitions.",
    "category": "shader",
    "category_group": "Marketing Blocks",
    "downloads": 6,
    "likes": 0
  }
}