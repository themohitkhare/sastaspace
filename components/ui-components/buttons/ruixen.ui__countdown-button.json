{
  "$schema": "https://ui.shadcn.com/schema/registry-item.json",
  "name": "countdown-button",
  "type": "registry:component",
  "title": "Countdown Button",
  "description": "A countdown-button component.",
  "dependencies": [],
  "registryDependencies": [
    "https://21st.dev/r/originui/button"
  ],
  "files": [
    {
      "path": "src/components/ui/countdown-button.tsx",
      "content": "import React, { useState, useEffect } from \"react\";\nimport { Button } from \"@/components/ui/button\";\nimport { cn } from \"@/lib/utils\";\n\ninterface CountdownButtonProps extends React.ButtonHTMLAttributes<HTMLButtonElement> {\n  label: string;\n  countdown: number; // in seconds\n  size?: \"sm\" | \"md\" | \"lg\";\n  onClick?: () => void;\n}\n\nconst sizeConfig = {\n  sm: \"px-3 py-1 text-sm\",\n  md: \"px-4 py-2 text-base\",\n  lg: \"px-5 py-3 text-lg\",\n};\n\nexport default function CountdownButton({ label, countdown, size = \"md\", onClick, className, ...props }: CountdownButtonProps) {\n  const [timeLeft, setTimeLeft] = useState(0);\n  const [disabled, setDisabled] = useState(false);\n\n  useEffect(() => {\n    let timer: NodeJS.Timeout;\n    if (timeLeft > 0) {\n      timer = setTimeout(() => setTimeLeft(timeLeft - 1), 1000);\n    } else {\n      setDisabled(false);\n    }\n    return () => clearTimeout(timer);\n  }, [timeLeft]);\n\n  const handleClick = () => {\n    if (onClick) onClick();\n    setTimeLeft(countdown);\n    setDisabled(true);\n  };\n\n  return (\n    <Button\n      className={cn(sizeConfig[size], className)}\n      onClick={handleClick}\n      disabled={disabled}\n      {...props}\n    >\n      {disabled ? `${label} in ${timeLeft}s` : label}\n    </Button>\n  );\n}\n",
      "type": "registry:component"
    }
  ],
  "_meta": {
    "author": "ruixen.ui",
    "slug": "countdown-button",
    "registry_url": "https://21st.dev/r/ruixen.ui/countdown-button",
    "title": "CountdownButton",
    "description": "The Countdown Button is a shadcn/ui component designed for time-sensitive actions, such as “Resend OTP” or retry mechanisms. Once clicked, the button starts a countdown timer and automatically disables itself, showing the remaining seconds (e.g., “Resend OTP in 20s”). After the countdown ends, it re-enables for further interactions. This component supports configurable sizes (sm, md, lg) and is highly reusable for workflows that require controlled user interactions, ensuring both clarity and functionality while maintaining a clean, modern UI.",
    "category": "button",
    "category_group": "UI Components",
    "downloads": 3,
    "likes": 0
  }
}