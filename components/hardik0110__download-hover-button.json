{
  "name": "download-hover-button",
  "type": "registry:ui",
  "dependencies": [
    "framer-motion"
  ],
  "registryDependencies": [],
  "files": [
    {
      "path": "/components/ui/download-hover-button.tsx",
      "content": "\"use client\"\r\n\r\nimport * as React from \"react\"\r\nimport { motion } from \"framer-motion\"\r\n\r\nexport default function AnimatedDownloadButton() {\r\n  const [isHovered, setIsHovered] = React.useState(false)\r\n\r\n  return (\r\n    <a \r\n      href=\"#Your Download Link\" \r\n      download\r\n      className=\"absolute center \"\r\n    >\r\n      <motion.div\r\n        initial={{ width: 64, height: 64 }}\r\n        whileHover={{ width: 220 }}\r\n        onHoverStart={() => setIsHovered(true)}\r\n        onHoverEnd={() => setIsHovered(false)}\r\n        transition={{ duration: 0.3 }}\r\n        className=\"bg-red-600 flex items-center justify-center overflow-hidden relative\"\r\n        style={{ borderRadius: 32 }}\r\n      >\r\n        <motion.div\r\n          className=\"absolute\"\r\n          animate={{ \r\n            opacity: isHovered ? 0 : 1,\r\n            scale: isHovered ? 0.8 : 1\r\n          }}\r\n          transition={{ duration: 0.2 }}\r\n        >\r\n          <span className=\"text-white text-2xl\">↓</span>\r\n        </motion.div>\r\n\r\n        <motion.div\r\n          className=\"w-full flex justify-center items-center\"\r\n          initial={{ opacity: 0 }}\r\n          animate={{ opacity: isHovered ? 1 : 0 }}\r\n          transition={{ duration: 0.2, delay: isHovered ? 0.1 : 0 }}\r\n        >\r\n          <span className=\"text-white text-lg font-bold whitespace-nowrap\">\r\n            Download \r\n          </span>\r\n        </motion.div>\r\n      </motion.div>\r\n    </a>\r\n  )\r\n}\r\n",
      "type": "registry:ui",
      "target": ""
    }
  ],
  "tailwind": {
    "config": {
      "theme": {
        "extend": {
          "transitionProperty": {
            "width": "width"
          }
        }
      }
    }
  },
  "_meta": {
    "author": "hardik0110",
    "slug": "download-hover-button",
    "registry_url": "https://21st.dev/r/hardik0110/download-hover-button",
    "title": "Download Hover Button",
    "description": "A rounded download button that smoothly expands on hover and collapses when not hovered, providing a clean, interactive experience.",
    "category": "button",
    "category_group": "UI Components",
    "downloads": 64,
    "likes": 0
  }
}