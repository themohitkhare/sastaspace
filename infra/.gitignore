keys/
data/
landing/out/
notes/out/
agents/moderator/.env
agents/moderator/.venv/
# Keep the dir (so the bind mount has a target on fresh clones) but
# ignore the generated zip contents.
deck-out/*
!deck-out/.gitkeep
localai/models/
