apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend
  namespace: sastaspace
spec:
  replicas: 1
  selector:
    matchLabels:
      app: backend
  template:
    metadata:
      labels:
        app: backend
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8080"
        prometheus.io/path: "/metrics"
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
      containers:
      - name: backend
        image: localhost:32000/sastaspace-backend:latest
        securityContext:
          allowPrivilegeEscalation: false
        ports:
        - containerPort: 8080
        envFrom:
        - configMapRef:
            name: sastaspace-config
        - secretRef:
            name: sastaspace-env
            optional: true
        - secretRef:
            name: sastaspace-db-auth
        env:
        - name: MODE
          value: "server"
        - name: CORS_ORIGINS
          value: "https://sastaspace.com"
        volumeMounts:
        - name: sites-data
          mountPath: /data/sites
        - name: scan-tmp
          mountPath: /tmp/scan
        readinessProbe:
          httpGet:
            path: /
            port: 8080
          initialDelaySeconds: 10
          periodSeconds: 5
        livenessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 30
          periodSeconds: 15
          failureThreshold: 3
        resources:
          requests:
            memory: "256Mi"
            cpu: "100m"
          limits:
            memory: "1Gi"
            cpu: "500m"
      # ClamAV sidecar disabled — official image requires root/chown which
      # violates the pod's runAsNonRoot policy. The asset validation pipeline
      # still has 4 layers (magic bytes, Pillow, defusedxml, YARA).
      # TODO: Re-enable with a non-root ClamAV image build.
      volumes:
      - name: sites-data
        persistentVolumeClaim:
          claimName: sites-pvc
      - name: scan-tmp
        emptyDir:
          sizeLimit: 256Mi
---
apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: sastaspace
spec:
  selector:
    app: backend
  ports:
  - port: 8080
    targetPort: 8080
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: sites-pvc
  namespace: sastaspace
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
